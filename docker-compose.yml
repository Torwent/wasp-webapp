version: "3.9"

services:
  wasp-webapp:
    container_name: "wasp-webapp"
    build:
      context: .
    restart: unless-stopped
    networks:
      - wasp-proxy

networks:
  wasp-proxy:
    external: true
