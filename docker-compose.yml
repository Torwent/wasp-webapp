ersion: "3.9"

services:
  container_name: "nodeserver"
  nodeserver:
    build:
      context: .
    ports:
      - "3000:3000"
    restart: unless-stopped
  networks:
    - proxy

networks:
  proxy:
    driver: bridge
